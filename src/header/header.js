import React from 'react';
import './header.css'; // Jika Anda ingin menambahkan styling khusus

const Header = () => {
    return (
        <header className="header">
            <h1>To-Do List</h1>
        </header>
    );
};

export default Header;